class Product {
  constructor(id, catId, title, imageUrl, details, price) {
    this.id = id;
    this.catId = catId;
    this.title = title;
    this.imageUrl = imageUrl;
    this.details = details;
    this.price = price;
  }
}

export default Product;
